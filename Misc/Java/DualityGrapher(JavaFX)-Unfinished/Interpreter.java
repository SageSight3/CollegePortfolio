public class Interpreter {

	//Is there another way to intereret user input for the function

	//convert sin input into Math.sin
	//convert cos input into Math.cos
	//convert tan input into Math.tan
	//convert a^b in input into Math.pow(a, b)
	//convert sqrt(x) into Math.sqrt(x)
	//convert rootC(x) into Math.pow(x, (1 / C))
	//convert log(x) into Math.log10(x)
	//convert ln(x) into Math.log(x)
	//convert logC(x) into Math.log(x) / Math.log(C)
	//convert abs(x) into Math.abs(x)
	//convert e into Math.E
	//convert pi into Math.PI

}